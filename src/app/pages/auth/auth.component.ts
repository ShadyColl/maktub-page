import { Component } from '@angular/core';

/// The redirect target for both Supabase email links. Task 3 makes it
/// branch confirm-landing vs password-recovery.
@Component({
  selector: 'app-auth',
  template: `<h1>Auth</h1>`,
})
export class AuthComponent {}
