import { Component } from '@angular/core';

/// Minimal landing — the growth seam for the future portfolio homepage.
@Component({
  selector: 'app-home',
  template: `<h1>maktub</h1>`,
})
export class HomeComponent {}
